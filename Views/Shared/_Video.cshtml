@inject IVideoRepository _videoRepo
@{
    var video = _videoRepo.Collection().Take(2);
}

@foreach (var item in video)
{

<video controls >
  <source src="~/Videos/@item.VideoUrl" type="video/mp4">
  Your browser does not support HTML video.
</video>

}
